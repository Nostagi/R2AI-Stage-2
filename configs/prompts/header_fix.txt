Bạn là một chuyên gia xử lý dữ liệu báo cáo tài chính.

Dưới đây là mảng 1 chiều chứa tên các cột (header) đã được làm phẳng từ cấu trúc multi-index của bảng Báo Cáo Tài Chính:
{flat_header}

YÊU CẦU:
1. Sửa các lỗi chính tả OCR trong tên từng cột (nếu có).
2. Loại bỏ các lặp từ hoặc gạch nối (_) thừa do lỗi merge cell gây ra (Ví dụ: "Năm 2023_Năm 2023_Số cuối kỳ" -> "Năm 2023_Số cuối kỳ").
3. BẮT BUỘC giữ nguyên đúng tổng số cột là {n_cols} phần tử. Không thêm, không bớt cột.
4. Trả về kết quả dưới dạng một JSON Array duy nhất chứa danh sách các chuỗi (strings). Không viết thêm bất kỳ lời giải thích hay ký tự markdown nào ngoài JSON.

OUTPUT (JSON Array):